package algo.todo.global.security

object SecurityConstants {
    val PERMIT_URLS =
        listOf(
            "/favicon.ico",
            "/login",
            "/error",
            "/v1/auth/refresh",
        )
}
